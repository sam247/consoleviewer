# Google OAuth for Search Console API
# Create OAuth 2.0 credentials in Google Cloud Console, enable Search Console API
# Add redirect URI: https://consoleviewer.vercel.app/api/auth/callback/google (and localhost for dev)
# Scope: https://www.googleapis.com/auth/webmasters.readonly
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://consoleviewer.vercel.app/api/auth/callback/google

# Get this by visiting /api/auth/google, signing in, then adding the shown value in Vercel env
GOOGLE_REFRESH_TOKEN=

# App login (optional). If set, visitors must sign in with this email/password to see the app.
# Use a long random string for APP_SESSION_SECRET (e.g. openssl rand -hex 32).
APP_LOGIN_EMAIL=
APP_LOGIN_PASSWORD=
APP_SESSION_SECRET=
